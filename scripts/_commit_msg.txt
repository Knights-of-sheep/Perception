feat(004): create_window 返回窗口 id + executeCommand 回显修复（2026-08-29 用户反馈）

返回值语义修订（user-validated）：
- create_window(title: str = "") -> str 返回新建子窗口的 id（"Plot_" + 序号）
- title 缺省（或空白）时 title = id（窗口标题显示 id）
- 传入 title 时窗口标题显示 title
- 参数类型错误抛 TypeError；宿主未连接返回 None 不崩溃
- 桥接：std::function<QString(const QString&)> 替代信号（Qt 信号无返回值）
- 窗口 id 命名 Plot_N（大写 P），取代此前"plot_ 小写 + 忽略入参"

executeCommand 回显错位修复：
- 旧实现强制在 prompt 行后插入换行导致命令独立成行（>>>\ncommand\nresult\n>>>）
- 新实现直接插入当前输入行（紧跟提示符），与手工输入行布局一致
- 仅当输入行已有用户输入时才换行（避免文本粘连）

代码：
- PythonConsole.h: requestCreateWindow 改为返回 QString（id），新增 setCreateWindowCallback
- PythonConsole.cpp: create_window_impl 支持可选 title 参数返回 id str；
  requestCreateWindow 调回调返回 id；executeCommand 改用 currentInput 判断换行
- MainWindow.h/.cpp: createSubwindow 返回 QString（"Plot_N"，title 缺省默认 = id）；
  菜单动作槽构造无参 create_window()；连接改为回调注册

文档（spec/契约/plan/tasks/quickstart）同步：True/False → 窗口 id，plot_ → Plot_，新增 2026-08-29
澄清记录（Clarifications 会话 2），US1 场景 5/FR-027/SC-018/Assumptions 命名修订

验证：构建通过，7/7 CTest 通过，快照确认 `>>> create_window()\n'Plot_1'\n>>> ` 无错位 +
返回 id + 子窗口标题 Plot_1
